# GCP Document AI Configuration (REST API)
GCP_DOCAI_ENDPOINT=https://us-documentai.googleapis.com/v1/projects/your-project-id/locations/us

# Authentication — choose ONE of the following methods:
#
# Option A: Service account key file (if your org allows key creation)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
#
# Option B: Application Default Credentials (recommended for local dev)
# Run: gcloud auth application-default login --project your-project-id
# Then leave GOOGLE_APPLICATION_CREDENTIALS commented out.

# Optional: logging level (DEBUG, INFO, WARNING, ERROR)
GCP_DOCAI_LOG_LEVEL=INFO
